# Dependencies
node_modules/

# Env and secrets (use *.example files in repo)
.env
.env.*
!.env*.example

# Build / cache
frontend/.next/
frontend/out/
backend/dist/
*.tsbuildinfo

# DB (локальная SQLite)
backend/prisma/*.db
backend/prisma/*.db-journal

# Logs / OS / IDE
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Prisma generated (опционально — если генерите на CI; иначе оставьте и закоммитьте после generate)
# backend/node_modules/.prisma/
